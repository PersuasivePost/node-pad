{
  "name": "node-pad-backend",
  "version": "1.0.0",
  "description": "Backend for collaborative notepad",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node -e \"const fs=require('fs'); fs.mkdirSync('dist',{recursive:true}); fs.copyFileSync('server.js','dist/server.js');\""
  },
  "dependencies": {
    "y-websocket": "^1.5.0",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
